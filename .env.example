# BiteStream :: copy to .env and adjust if your servers are not on localhost defaults.
# Every script reads these; none of them hardcode a host or password.
PGHOST=127.0.0.1
PGPORT=5432
PGDATABASE=bitestream
PGUSER=bs
PGPASSWORD=bs

MONGO_URI=mongodb://127.0.0.1:27017
MONGO_DB=bitestream
